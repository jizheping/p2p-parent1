spring:
  application:
    name: p2p-registry-eureka
server:
  port: 8080
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8080/eureka/
    register-with-eureka: false
    fetch-registry: false